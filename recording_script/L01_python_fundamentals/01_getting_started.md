Let's get started with writing some code

Historical conventions dictate that whenever we learn a new programming language, like Python, that we write something called a "Hello, world" program. We keep with this tradition by writing our own "Hello, world" program below:

We will learn some of the basics of how to code by referencing this program throughout this video

<Talk briefly about the code>

<Talk about variable assignment>

* Variables are used so that we can reference them later -- They only last as long as the Python session lasts
* Cannot refer to variables that have not yet been defined
* Order of operations on how a variable gets created
* Warning that we can overwrite previously defined variables

<Talk about code comments>

* Code comments are for yourself as much as they are for others
* Many cases where you will make a clever realization about the best way to do something but, if you don't write down a comment, you might need to figure it out again when something goes wrong.

<Talk about calling functions>

* Functions allow us to bake general ideas into a function which allows us to write less code
* Python has pre-defined a handful of useful functions for us
  - Don't name a variable one of these pre-defined functions...
* Getting help

